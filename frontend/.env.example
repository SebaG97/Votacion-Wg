VITE_API_BASE_URL=http://localhost:8000/api/v1

# Link del footer al panel administrativo (frontend-admin/). Sin configurar,
# en desarrollo apunta a http://localhost:5174 (puerto por defecto de
# frontend-admin/); en un build de produccion cae a la ruta relativa /admin
# (pensada para cuando los dos frontends se sirvan bajo el mismo dominio,
# por ejemplo como dos componentes de una misma app de DigitalOcean App
# Platform con rutas "/" y "/admin").
VITE_ADMIN_PANEL_URL=
